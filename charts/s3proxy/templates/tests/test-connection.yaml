apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "comet-common.names.fullname" . }}-test-connection"
  labels:
    {{- include "comet-common.labels.base" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "comet-common.names.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
